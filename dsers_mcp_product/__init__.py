"""
DSers MCP Product Package
DSers MCP 商品包

This package exposes a provider-neutral, scenario-first product import
workflow as an MCP server. The protocol layer is fully open-source and
never contains platform-specific API logic — all vendor details are
injected at runtime through the ImportProvider interface.

本包通过 MCP 服务器对外暴露一套与平台无关的、场景优先的商品导入工作流。
协议层完全开源，不包含任何特定平台 API 逻辑 —— 所有供应商细节在运行时
通过 ImportProvider 接口注入。
"""
