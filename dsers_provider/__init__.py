"""
Private DSers Provider Adapter — Bridges the public MCP to the DSers platform.
私有 DSers 提供者适配器 —— 桥接公开 MCP 与 DSers 平台

This package contains the concrete ImportProvider implementation that
translates the provider-neutral import protocol into DSers-specific
API calls. It is intentionally kept private — the source code may expose
internal API structures and should not be published to open-source
repositories without sanitisation.

本包包含具体的 ImportProvider 实现，将平台无关的导入协议转换为
DSers 特定的 API 调用。此代码有意保持私有 —— 源码可能暴露内部
API 结构，未经脱敏不应发布到开源仓库。
"""
